import { Href } from 'expo-router/build/types'; type AuthRoutes = '/(auth)/login' | '/(auth)/register'; type TabRoutes = '/(tabs)' | '/(tabs)/profile' | '/(tabs)/index' | '/(tabs)/secured' | '/(tabs)/finances' | '/(tabs)/videos'; type SettingsRoutes = '/(settings)' | '/(settings)/subscription' | '/(settings)/terms' | '/(settings)/privacy'; type DodjeOneRoutes = '/(dodjeone)' | '/dodjeone'; type CourseRoutes = `/course/${string}`; type VideoRoutes = `/video/${string}`; export type AppRoute = Href & ('/' | AuthRoutes | TabRoutes | SettingsRoutes | DodjeOneRoutes | CourseRoutes | VideoRoutes); export type RouteParams = { '/course/[id]': { id: string }; '/video/[id]': { id: string }; }; export type HasParams<T extends AppRoute> = T extends keyof RouteParams ? true : false; export type GetRouteParams<T extends AppRoute> = T extends keyof RouteParams ? RouteParams[T] : never;
